#include <stdlib.h>
#include "../src/sum.h"

/**
 * @brief 
 * below will return 0 
 * which will be treated pass condition by ctest
 * @return int 
 */
int test_sum_valid_input_passes();

/**
 * @brief 
 * below will return 0 
 * as the condition fails and ctest will result as pass
 * @return int 
 */
int test_sum_invalid_input_fails();